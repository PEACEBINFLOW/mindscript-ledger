# 8.1-mindscript-core.md

MindScript Core → Ledger:

- Every MindScript program can log:
  - the raw prompt
  - the normalized internal representation
  - the pattern hash
  - the stage transitions

This enables reconstructing MindScript execution later.
