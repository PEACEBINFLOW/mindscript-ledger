# 8.2-mindscript-templates.md

Templates integration:

- Each template can have a `template_id`
- Ledger tracks which templates are used in which threads
- You can query:
  - "show me all sessions using template X"
